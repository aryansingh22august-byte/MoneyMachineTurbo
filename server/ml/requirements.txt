# Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Machine Learning
scikit-learn>=1.3.0
joblib>=1.3.0
torch>=2.1.0
huggingface-hub>=0.19.0

# Kronos foundation model (server/ml/model/*).
# These were previously missing, so `from model.kronos import ...` always raised
# ImportError. ml_api.py catches that and silently sets KRONOS_AVAILABLE=False,
# so Kronos never actually loaded in Docker despite appearing fully wired up.
einops>=0.7.0
tqdm>=4.66.0

# Market Data — for auto-training on startup without needing Node.js to push data
yfinance>=0.2.36

# API Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Utilities
pydantic>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# Removed: xgboost, transformers. Neither is imported anywhere under server/ml
# (advanced_predictor.py uses torch + scikit-learn; Kronos loads through
# huggingface_hub's PyTorchModelHubMixin). Both are large wheels that were
# adding build time and image size for nothing.
